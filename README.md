# Mafia Night

A dark social deduction game starter built with Expo + React Native + TypeScript.

## Run

```bash
npm install
npx expo start
```

The UI is ready for the next phase: Supabase authentication, rooms, realtime players, roles, chat, missions and VIP.
